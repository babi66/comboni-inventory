---
import '../styles/global.css';
---

<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Comboni Executive Analytics</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
</head>
<body class="bg-slate-50 text-slate-900 font-sans leading-relaxed">
    
    <!-- GLASS NAVIGATION: Transparent to let hero show through -->
    <!-- GLASS NAVIGATION -->
    <nav class="fixed top-0 left-0 right-0 z-[100] bg-white/10 backdrop-blur-md border-b border-white/20 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="flex items-center gap-3">
                <!-- CORRECTED PATH HERE -->
                <img src="/images/blocks/logo.jfif" alt="Logo" class="w-10 h-10 object-contain drop-shadow-md" />
                <div class="flex flex-col">
                    <span class="font-black text-white leading-none text-sm uppercase tracking-tighter">Comboni</span>
                    <span class="text-[10px] font-bold text-blue-300 uppercase tracking-widest">Analytics Portal</span>
                </div>
            </div>
            <div class="flex items-center gap-2">
                <a href="/" class="text-[10px] font-black text-white/80 hover:text-white px-3 py-2 rounded-xl uppercase tracking-widest">Register</a>
                <div class="w-px h-4 bg-white/20 mx-2"></div>
                <button id="logout-btn" class="text-[10px] font-black text-red-400 hover:bg-red-500 hover:text-white px-4 py-2 rounded-xl uppercase tracking-widest">Logout</button>
            </div>
        </div>
    </nav>

    <!-- HERO SECTION (Logo removed from center for better text visibility) -->
    <header class="relative h-[500px] bg-blue-950 flex items-center justify-center text-center px-4 overflow-hidden">
        <img src="/images/blocks/block1.jpg" 
             class="absolute inset-0 w-full h-full object-cover opacity-30" />
        
        <div class="absolute inset-0 bg-gradient-to-t from-slate-50 via-blue-950/60 to-transparent"></div>
        
        <!-- text container shifted down slightly to clear the fixed nav -->
        <div class="relative z-10 flex flex-col items-center mt-16">
            <span class="text-blue-300 font-black tracking-[0.5em] text-[10px] uppercase mb-4 block drop-shadow-md">
                Official 2018 E.C. Ledger
            </span>
            <h1 class="text-5xl md:text-7xl font-black text-white mb-2 drop-shadow-2xl tracking-tighter uppercase">
                Inventory <span class="text-blue-400">Control</span>
            </h1>
            <p class="text-blue-100 text-sm font-medium max-w-2xl mx-auto italic opacity-90 uppercase tracking-[0.2em]">
                "Visualizing every asset, Block by Block"
            </p>
        </div>
    </header>

    <!-- MAIN CONTENT: Reduced negative margin (-mt-16 instead of -mt-28) to stop overlap -->
    <main class="container mx-auto px-6 relative z-20 -mt-16">
        
        <!-- Global Stat Cards -->
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4 mb-16">
            <div class="bg-white p-6 rounded-[2rem] shadow-2xl border-b-4 border-blue-600 hover:-translate-y-2 hover:shadow-blue-200/50 transition-all duration-300 group cursor-default">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1 group-hover:text-blue-600 transition-colors">Total Assets</p>
                <h2 id="grand-total" class="text-3xl font-black text-slate-800 tracking-tighter">0</h2>
            </div>
            
            <div class="bg-white p-6 rounded-[2rem] shadow-2xl border-b-4 border-emerald-500 hover:-translate-y-2 hover:shadow-emerald-200/50 transition-all duration-300 group cursor-default">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1 group-hover:text-emerald-500 transition-colors">Functional</p>
                <h2 id="func-total" class="text-3xl font-black text-emerald-600 tracking-tighter">0</h2>
            </div>

            <div class="bg-white p-6 rounded-[2rem] shadow-2xl border-b-4 border-blue-400 hover:-translate-y-2 transition-all duration-300 group cursor-default">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1 group-hover:text-blue-400">Chairs</p>
                <h2 id="chairs-total" class="text-3xl font-black text-slate-800 tracking-tighter">0</h2>
            </div>

            <div class="bg-white p-6 rounded-[2rem] shadow-2xl border-b-4 border-blue-400 hover:-translate-y-2 transition-all duration-300 group cursor-default">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1 group-hover:text-blue-400">Tables</p>
                <h2 id="tables-total" class="text-3xl font-black text-slate-800 tracking-tighter">0</h2>
            </div>

            <div class="bg-white p-6 rounded-[2rem] shadow-2xl border-b-4 border-blue-400 hover:-translate-y-2 transition-all duration-300 group cursor-default">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1 group-hover:text-blue-400">Computers</p>
                <h2 id="pcs-total" class="text-3xl font-black text-slate-800 tracking-tighter">0</h2>
            </div>

            <button id="export-btn" class="group bg-slate-900 text-white p-6 rounded-[2rem] shadow-2xl hover:bg-blue-600 hover:-translate-y-2 hover:shadow-blue-400/40 transition-all duration-300 flex flex-col justify-center items-center relative overflow-hidden">
                <span class="text-[9px] font-black uppercase opacity-70 tracking-tighter">Report</span>
                <span class="text-sm font-extrabold uppercase tracking-tighter">Excel</span>
            </button>
        </div>

        <div id="block-container" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
            <!-- JS Injected Content -->
        </div>

    </main>

   <!-- SYSTEM FOOTER -->
<footer class="mt-20 pb-10 border-t border-slate-200/50">
    <div class="container mx-auto px-6">
        <div class="flex flex-col md:flex-row justify-between items-center gap-6 pt-10">
            
            <!-- System Info -->
            <div class="flex items-center gap-4">
                <img src="/images/blocks/logo.jfif" alt="Logo" class="w-8 h-8 grayscale opacity-50" />
                <div class="flex flex-col">
                    <span class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Comboni Inventory Management</span>
                    <span class="text-[9px] font-medium text-slate-400 italic">Official 2018 E.C. Ledger System</span>
                </div>
            </div>

            <!-- Developer Credit -->
            <div class="flex flex-col items-center md:items-end">
                <span class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1">Architected & Developed by</span>
                <a href="https://my-portfolio-two-wine-86.vercel.app/" 
                   target="_blank" 
                   rel="noopener noreferrer"
                   class="group flex items-center gap-2 bg-slate-900 text-white px-5 py-2.5 rounded-2xl hover:bg-blue-600 transition-all duration-300 shadow-lg shadow-slate-200">
                    <span class="text-[10px] font-black uppercase tracking-widest">View Portfolio</span>
                    <svg class="w-3 h-3 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                    </svg>
                </a>
            </div>

        </div>

        <!-- Bottom Bar -->
        <div class="mt-10 pt-6 border-t border-slate-100 flex justify-center">
            <p class="text-[8px] font-bold text-slate-300 uppercase tracking-[0.5em]">
                &copy; 2026 Comboni Schools &bull; All Rights Reserved
            </p>
        </div>
    </div>
</footer>

    <script>
        import { db, auth } from '../lib/firebase';
        import { collection, onSnapshot } from "firebase/firestore";
        import { signOut, onAuthStateChanged } from "firebase/auth";
        import * as XLSX from 'xlsx';

        let currentAssets = [];

        onAuthStateChanged(auth, (user) => { if (!user) window.location.href = "/login"; });
        document.getElementById('logout-btn')?.addEventListener('click', () => {
            signOut(auth).then(() => window.location.href = "/login");
        });

        const blockDetails = [
            { id: 1, label: "Administration", info: "Offices & WC", img: "/images/blocks/block1.jpg" },
            { id: 2, label: "Grade 10", info: "Classrooms", img: "/images/blocks/block2.jpg" },
            { id: 3, label: "Grade 9", info: "Classrooms", img: "/images/blocks/block3.jpg" },
            { id: 4, label: "Science Labs", info: "Chem, Bio, Physics", img: "/images/blocks/block4.jpg" },
            { id: 5, label: "ICT & TV Room", info: "Tech Center", img: "/images/blocks/block5.jpg" },
            { id: 6, label: "Library", info: "Study Areas", img: "/images/blocks/block6.jpg" },
            { id: 7, label: "Drawing Bldg", info: "Technical Art", img: "/images/blocks/block7.jpg" },
            { id: 8, label: "Grade 11", info: "Classrooms", img: "/images/blocks/block8.jpg" },
            { id: 9, label: "Grade 12 & ICT 1", info: "Offices & CTE", img: "/images/blocks/block9.jpg" },
            { id: 10, label: "Services", info: "Restrooms", img: "/images/blocks/block10.jpg" },
            { id: 11, label: "Pedagogy", info: "Training Center", img: "/images/blocks/block11.jpg" }
        ];

        onSnapshot(collection(db, "assets"), (snapshot) => {
            currentAssets = snapshot.docs.map(doc => doc.data());
            
            const total = currentAssets.reduce((s, a) => s + Number(a.quantity || 0), 0);
            const functional = currentAssets.filter(a => a.condition === 'Functional').reduce((s, a) => s + Number(a.quantity || 0), 0);
            const chairs = currentAssets.filter(a => a.description.toLowerCase().includes('chair')).reduce((s, a) => s + Number(a.quantity || 0), 0);
            const tables = currentAssets.filter(a => a.description.toLowerCase().includes('table')).reduce((s, a) => s + Number(a.quantity || 0), 0);
            const pcs = currentAssets.filter(a => a.description.toLowerCase().includes('pc') || a.description.toLowerCase().includes('computer')).reduce((s, a) => s + Number(a.quantity || 0), 0);

            document.getElementById('grand-total').innerText = total;
            document.getElementById('func-total').innerText = functional;
            document.getElementById('chairs-total').innerText = chairs;
            document.getElementById('tables-total').innerText = tables;
            document.getElementById('pcs-total').innerText = pcs;

            const container = document.getElementById('block-container');
            container.innerHTML = blockDetails.map(block => {
                const blockAssets = currentAssets.filter(a => a.block === `Block ${block.id}`);
                const count = blockAssets.reduce((s, a) => s + Number(a.quantity || 0), 0);
                return `
                <div class="bg-white rounded-[2rem] shadow-lg border border-slate-200 overflow-hidden hover:shadow-2xl transition-all group">
                    <div class="h-44 relative">
                        <img src="${block.img}" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
                        <div class="absolute inset-0 bg-gradient-to-t from-slate-900/80 to-transparent"></div>
                        <div class="absolute bottom-4 left-4 text-white">
                            <span class="text-[10px] font-bold uppercase tracking-widest opacity-80">Block ${block.id}</span>
                            <h3 class="text-xl font-bold uppercase tracking-tighter">${block.label}</h3>
                        </div>
                    </div>
                    <div class="p-6">
                        <div class="flex justify-between items-center mb-4">
                            <span class="text-xs text-slate-500 font-bold uppercase">${block.info}</span>
                            <span class="bg-blue-50 text-blue-700 px-3 py-1 rounded-full text-[10px] font-black">${count} ITEMS</span>
                        </div>
                        <details class="group/det">
                            <summary class="list-none cursor-pointer text-blue-600 font-black text-[10px] flex items-center gap-1 group-open/det:mb-4 uppercase tracking-widest">
                                <svg class="w-3 h-3 transition-transform group-open/det:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="4" d="M19 9l-7 7-7-7"></path></svg>
                                Details
                            </summary>
                            <div class="space-y-2 max-h-60 overflow-y-auto pr-2">
                                ${generateSections(blockAssets)}
                            </div>
                        </details>
                    </div>
                </div>
                `;
            }).join('');
        });

        function generateSections(assets) {
            const groups = {};
            assets.forEach(a => {
                const sec = a.section || "General";
                if (!groups[sec]) groups[sec] = [];
                groups[sec].push(a);
            });
            return Object.entries(groups).map(([sec, items]) => `
                <div class="bg-slate-50 p-3 rounded-2xl border border-slate-100">
                    <div class="flex justify-between items-center mb-1">
                        <span class="font-black text-slate-700 text-[9px] uppercase tracking-tighter">Section ${sec}</span>
                        <span class="text-[8px] font-black bg-white px-2 py-0.5 rounded shadow-sm text-slate-500 border border-slate-200">
                            ${items.reduce((s, i) => s + Number(i.quantity || 0), 0)} Qty
                        </span>
                    </div>
                    <div class="text-[10px] text-slate-500 leading-tight">
                        ${items.map(i => `${i.quantity}x ${i.description}`).join(', ')}
                    </div>
                </div>
            `).join('');
        }

        document.getElementById('export-btn')?.addEventListener('click', () => {
            if (currentAssets.length === 0) return alert("No data to export!");

            // 1. Main Heading
            const title = [["COMBONI SCHOOL FIXED ASSETS 2018 E.C"]];
            
            // 2. Metadata Heading
            const date = [["Report Date: " + new Date().toLocaleDateString()]];
            
            // 3. Spacing Row
            const empty = [[]];
            
            // 4. SUB-HEADINGS (These are the column titles you were missing)
            const columnHeadings = [["Block", "Section/Room", "Item Description", "Quantity", "Condition"]];
            
            // 5. Data Rows
            const rows = currentAssets.map(d => [
                d.block || "N/A", 
                d.section || "General", 
                d.description || "Unnamed Item", 
                d.quantity || 0, 
                d.condition || "N/A"
            ]);

            // Combine everything into the final sheet array
            const finalData = [
                ...title, 
                ...date, 
                ...empty, 
                ...columnHeadings, 
                ...rows
            ];

            // Create Worksheet
            const ws = XLSX.utils.aoa_to_sheet(finalData);

            // Centering the Main Heading (Merge A1 to E1)
            ws["!merges"] = [
                { s: { r: 0, c: 0 }, e: { r: 0, c: 4 } } 
            ];

            // Set Column Widths for better readability
            ws["!cols"] = [
                { wch: 15 }, // Block
                { wch: 20 }, // Section
                { wch: 40 }, // Description
                { wch: 10 }, // Quantity
                { wch: 15 }  // Condition
            ];

            const wb = XLSX.utils.book_new();
            XLSX.utils.book_append_sheet(wb, ws, "Assets_2018_EC");

            // Save File
            XLSX.writeFile(wb, "Comboni_Fixed_Assets_2018EC.xlsx");
        });
    </script>
</body>
</html>